{
  "installCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && $HOME/flutter/bin/flutter doctor",
  "buildCommand": "$HOME/flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
