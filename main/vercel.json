{
  "version": 2,
  "buildCommand": "git clone --depth 1 https://github.com -b stable flutter_sdk && ./flutter_sdk/bin/flutter build web --release --allow-root",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
